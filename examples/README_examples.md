# Examples

This folder is a placeholder for future examples such as:
- Text classification with bag-of-words counts and `MultinomialNB`.
- Binary feature datasets (e.g., yes/no indicators) with `BernoulliNB`.
- Comparisons against logistic regression on simple datasets.
